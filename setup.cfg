[flake8]
# E402: "module level import not at top of file" (because we need to do torch.set_default_dtype)
# W503: line break before binary operator (preferred style; W504 conflicts with it)
# W504: line break after binary operator (conflicts with W503, which is preferred)
ignore = E402, W503, W504
max-line-length = 120
